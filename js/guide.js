WebApp.Init('wxdca9f5edc034c1a0',//appid
   function(result,data){//result:请求状态,data 请求结果
      console.log(Options.GetUserInfo());
    //   $('#test').html(JSON.stringify(Options.GetUserInfo()));
   }
);